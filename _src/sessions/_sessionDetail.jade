article.session
    h1 #{name}
    if speaker && public.speakers[speaker]
        h2
            a(href='/speakers/#{speaker}') #{public.speakers[speaker]._data.index.name}
    h3 #{room}
    h3 #{timeslot}
    p
        != description
    